services:
  app:
    build: 
      context: .
      args:
        BUILDPLATFORM: linux/arm64
    platform: linux/arm64
    ports:
      - "3000:3000"
    environment:
      - MONGO_DB_URL=${MONGO_DB_URL}
    volumes:
      - .:/app
      - /app/node_modules